language: objective-c
before_install:
  - sudo chown -R travis ~/Library/RubyMotion
  - mkdir -p ~/Library/RubyMotion/build
  - sudo motion update
  - gem install motion-cocoapods --no-rdoc --no-ri --no-document --quiet
  - bundle exec rake pod:install
